{
  "name": "blog-generator",
  "version": "1.0.0",
  "description": "Blog generator from markdown files",
  "main": "generate.js",
  "scripts": {
    "generate": "node generate.js",
    "watch": "node generate.js --watch"
  },
  "dependencies": {
    "marked": "^11.1.1",
    "front-matter": "^4.0.2"
  }
}

