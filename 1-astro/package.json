{
  "name": "pixel-trail",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro check && astro build",
    "preview": "astro preview --host 127.0.0.1"
  },
  "dependencies": { "astro": "7.3.1" },
  "devDependencies": { "@astrojs/check": "^0.9.4", "typescript": "^5.9.3", "@playwright/test": "1.63.0" }
}
